
# check python installation, create config directory, create start script
# cd line required when running from finder
cd `dirname $0`
python src/config.py
echo "config completed, can close terminal window"
